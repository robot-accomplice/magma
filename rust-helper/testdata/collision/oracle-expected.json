{
  "fatal": [
    {
      "symbol": "dead_method",
      "pkg": "collision::dead_side",
      "file": "src/main.rs",
      "line": 12,
      "column": 12,
      "reason": "Task-9 residual gap: trait-declaration node (trait_impl:null) of a method whose whole trait+impl+type cluster is independently dead. rustc collapses the whole-cluster-dead case to one diagnostic per item ('trait `Amb` is never used' on the trait's own name line 11, 'struct `Widget` is never constructed' on line 14) and emits nothing on dead_method's own declaration line (12) at either the trait-decl or impl copy — confirmed empirically, only 3 dead_code diagnostics exist for this whole crate, none naming dead_method. The trait-impl-cascade exclusion only ever covers impl nodes (trait_impl set); this is the trait-decl copy, architecturally uncovered by that mechanism."
    },
    {
      "symbol": "live_method",
      "pkg": "collision::live_side",
      "file": "src/main.rs",
      "line": 22,
      "column": 12,
      "reason": "Trait-declaration node (trait_impl:null) with no incoming call-graph edge: calls to live_method resolve statically to the impl's own node (id=5, src/main.rs:27), never to this trait-declaration copy, so the helper marks this node unreachable even though live_method genuinely is live. rustc has no diagnostic here either (live_method is genuinely used, via the impl) so oracle_dead is also false at this line -- helper_dead=true/oracle_dead=false is the FATAL shape, but both sides agree the underlying method is live; only this trait-decl copy's own reachability is structurally unrepresentable."
    },
    {
      "symbol": "never_used",
      "pkg": "collision::live_side",
      "file": "src/main.rs",
      "line": 28,
      "column": 12,
      "reason": "Impl node (trait_impl set, trait `Amb`@21:15 NOT independently dead so the cascade does not fire) for a method that is genuinely unused. rustc attributes its single 'method `never_used` is never used' diagnostic to the TRAIT's own method-declaration line (src/main.rs:23, the trait-decl copy id=4, which correctly lands in agree_dead) rather than to this impl's own declaration line (28) -- confirmed directly against cargo check's JSON output. This impl copy therefore has no diagnostic at its own key and surfaces as a disclosed FATAL."
    }
  ]
}
